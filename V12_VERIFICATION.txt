KCSC V12 FINAL VERIFIED SERVICE CATALOGUE FIX
- Bundled MASTER_SERVICES count: 138
- All Services renders bundled catalogue before any database call.
- Supabase/database permission failure cannot blank the catalogue.
- Admin database service overrides are merged after initial render when available.
- Admin-created extra published services are appended.
- Service actions use database when available, bundled actions otherwise.
- portal.js uses local portal/assets/master-catalog.js to avoid parent-path/module issues.
